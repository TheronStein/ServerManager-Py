# Initialize an empty dictionary to store menu options
menus = {}

menus["Main_Menu"] = [
    "Start Servers",
    "Shutdown Servers",
    "Reset Servers",
    "Server Status",
    "Server Troubleshooting"
    # Add more options up to the limit defined in menu_info["Server_Start"]
    # Add options for another menu, limited by menu_info["Another_Menu"]
]

menus["Confirmation_Menu"] = [
    "Yes",
    "No"
    # Add more options up to the limit defined in menu_info["Server_Start"]
    # Add options for another menu, limited by menu_info["Another_Menu"]
]

# Define menu options
menus["Server_Start_Main"] = [
    "Start New Server",
    "Start New Set of Servers"
    # Add more options up to the limit defined in menu_info["Server_Start"]
]

# Define menu options
menus["Server_Start_Main"] = [
    "Start New Server",
    "Start New Set of Servers"
    # Add more options up to the limit defined in menu_info["Server_Start"]
]

# Define menu options
menus["Server_Start_New"] = [
    "Start New Server",
    "Start New Set of Servers"
    # Add more options up to the limit defined in menu_info["Server_Start"]
]

# Define menu options
menus["Server_Start_NewSet"] = [
    "Start New Server",
    "Start New Set of Servers"
    # Add more options up to the limit defined in menu_info["Server_Start"]
]

# Define menu options
menus["Server_Shutdown_Main"] = [

    "Shutdown Specific Server",
    "Shutdown All Servers"
    # Add more options up to the limit defined in menu_info["Server_Start"]
]
